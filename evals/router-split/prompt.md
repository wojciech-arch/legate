The `mini-cli` needs three more commands:

- `mini upper <text>` — prints the text uppercased
- `mini reverse <text>` — prints the text reversed
- `mini count <text>` — prints the number of characters

Each one needs tests.

And while you're in there: every command should print `missing argument for <command>`
and exit 1 when it's called with no argument — including the two that already exist.

Get it all working and the suite green.
