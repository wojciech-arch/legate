Please add a `version` command to this `mini-cli` project. Running `mini version`
should print the version string from `package.json` (currently `0.3.1`) and nothing
else on that line.

I want this done properly, not just typed in:

- The implementation should be test-driven — a test that pins the behavior, added and
  seen to fail first, then the code that makes it pass.
- Before you tell me it's done, I want it independently verified against what I asked
  for — not just a report that it works. Confirm the command actually prints the
  version and that the existing `greet`/`sum` behavior and tests still pass.

Tell me it's complete only once that verification has actually happened, and show me
the evidence it's based on.
