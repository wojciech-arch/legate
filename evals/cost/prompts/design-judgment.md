Read the `mini-cli` fixture and propose how it should be restructured to support
plugins that can register their own subcommands at runtime. Weigh at least two
designs (e.g. a static registry vs. dynamic directory scanning), call out the
trade-offs in coupling, testability, and startup cost, and recommend one with your
reasoning. Do not write any code — I want the design judgment, not an implementation.
