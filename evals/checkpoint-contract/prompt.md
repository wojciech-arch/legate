Please add an `upper` command to this `mini-cli` project. Running `mini upper hello`
should print `HELLO` and nothing else on that line. Do it test-driven — a test that
pins the behavior, added and seen to fail first, then the code that makes it pass.

I'm going to be working on something else while this runs, and I don't want to sit
blind until it's finished. Set it up so I can check how far along it is at any point —
without interrupting whatever is mid-flight, and without having to come and ask it.

When it's finished, show me the evidence, not just a summary.
